# Production tools

- `render/` - book, guide and slide rendering
- `qa/` - structural, visual, provenance and conformance checks
- `video/` - narration, subtitle and video assembly

Tooling is introduced only when its output is reproducible and source artifacts remain editable.
